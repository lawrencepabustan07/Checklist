# ZAP backend scan rule configuration
# The temporary CI HTTP server leaks its own Server banner; ignore that scan-environment noise.
10036	IGNORE	(Server Leaks Version Information via "Server" HTTP Response Header Field)
